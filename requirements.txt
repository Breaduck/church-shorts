# 유튜브가 자주 바뀌어 오래된 yt-dlp는 403(다운로드 거부)이 난다. 오류 나면 pip install -U yt-dlp.
yt-dlp>=2026.8.19
faster-whisper>=1.0.0
numpy>=1.26.0
librosa>=0.10.0
soundfile>=0.12.0
PyYAML>=6.0
# fonts.py가 폰트 name 테이블에서 family 이름을 읽고(ASS Fontname), captions.py가 제목
# 크기를 실측(_fit_title_font_size)하는 데 쓴다. 최상단 import라 없으면 편집기가 아예 안 뜬다.
fonttools>=4.50
Flask>=3.0.0
python-dotenv>=1.0.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
# 얼굴 추적 리프레이밍(facetrack): Haar 캐스케이드 얼굴 검출. 4.x는 캐스케이드 XML을
# 번들하지만, 리포에도 assets/facetrack에 복사해 버전 독립적으로 로드한다.
opencv-python-headless>=4.8,<5
# 회귀 테스트(tests/): venv\Scripts\python.exe -m pytest tests -q
pytest>=8.0
